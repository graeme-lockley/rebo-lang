let prime?(n = 0) = {
  let testPrime(i = 2) =
    if i * i > n -> true
     | n % i == 0 -> false
     | testPrime(i + 1)

  if n < 2 -> false
   | testPrime()
}

let nextLargestPrime(n) =
  if prime?(n) -> n
   | nextLargestPrime(n + 1)
